<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/bookheart_launcher" android:roundIcon="@mipmap/bookheart_launcher_round" android:supportsRtl="true">
        <!-- Firebase collection starts OFF (fail-closed). The platform must NOT auto-collect
             first_open/session_start/crash data before the consent gate resolves. The runtime
             gate (AnalyticsConsentGate → MainActivity.InitializeFirebase) flips collection ON
             via SetAnalyticsCollectionEnabled/SetCrashlyticsCollectionEnabled only after it has
             confirmed the user's persisted consent=true. See code review SEC-01. -->
        <meta-data android:name="firebase_analytics_collection_enabled" android:value="false" />
        <meta-data android:name="firebase_crashlytics_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_adid_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_ssaid_collection_enabled" android:value="false" />
        <!-- Live reading-timer foreground service. The [Service] attribute generates the base
             entry (same android:name); this entry contributes the required specialUse subtype
             property. The manifest merger combines them by name. -->
        <service
            android:name="com.bookheart.app.ReadingTimerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="live_reading_session_timer" />
        </service>
    </application>
    <!-- Android 11+ package visibility: declare email intents so Email.ComposeAsync /
         Launcher.OpenAsync(mailto:) can resolve an installed email client (used by the
         "Suggest a feature" Premium form). Without this, the launch fails silently. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />
</manifest>
